State(Data state,Consume the next input character:,List(Trance(U+0026 AMPERSAND (&amp;),Set the return state to the data state. Switch to the character reference state.), Trance(U+003C LESS-THAN SIGN (&lt;),Switch to the tag open state.), Trance(U+0000 NULL,This is an unexpected-null-character parse error. Emit the current input character as a character token.), Trance(EOF,Emit an end-of-file token.), Trance(Anything else,Emit the current input character as a character token.)))
State(RCDATA state,Consume the next input character:,List(Trance(U+0026 AMPERSAND (&amp;),Set the return state to the RCDATA state. Switch to the character reference state.), Trance(U+003C LESS-THAN SIGN (&lt;),Switch to the RCDATA less-than sign state.), Trance(U+0000 NULL,This is an unexpected-null-character parse error. Emit a U+FFFD REPLACEMENT CHARACTER character token.), Trance(EOF,Emit an end-of-file token.), Trance(Anything else,Emit the current input character as a character token.)))
State(RAWTEXT state,Consume the next input character:,List(Trance(U+003C LESS-THAN SIGN (&lt;),Switch to the RAWTEXT less-than sign state.), Trance(U+0000 NULL,This is an unexpected-null-character parse error. Emit a U+FFFD REPLACEMENT CHARACTER character token.), Trance(EOF,Emit an end-of-file token.), Trance(Anything else,Emit the current input character as a character token.)))
State(Script data state,Consume the next input character:,List(Trance(U+003C LESS-THAN SIGN (&lt;),Switch to the script data less-than sign state.), Trance(U+0000 NULL,This is an unexpected-null-character parse error. Emit a U+FFFD REPLACEMENT CHARACTER character token.), Trance(EOF,Emit an end-of-file token.), Trance(Anything else,Emit the current input character as a character token.)))
State(PLAINTEXT state,Consume the next input character:,List(Trance(U+0000 NULL,This is an unexpected-null-character parse error. Emit a U+FFFD REPLACEMENT CHARACTER character token.), Trance(EOF,Emit an end-of-file token.), Trance(Anything else,Emit the current input character as a character token.)))
State(Tag open state,Consume the next input character:,List(Trance(U+0021 EXCLAMATION MARK (!),Switch to the markup declaration open state.), Trance(U+002F SOLIDUS (/),Switch to the end tag open state.), Trance(ASCII alpha,Create a new start tag token, set its tag name to the empty string. Reconsume in the tag name state. ), Trance(U+003F QUESTION MARK (?),This is an unexpected-question-mark-instead-of-tag-name parse error. Create a comment token whose data is the empty string. Reconsume in the bogus comment state.), Trance(EOF,This is an eof-before-tag-name parse error. Emit a U+003C LESS-THAN SIGN character token and an end-of-file token.), Trance(Anything else,This is an invalid-first-character-of-tag-name parse error. Emit a U+003C LESS-THAN SIGN character token. Reconsume in the data state.)))
State(End tag open state,Consume the next input character:,List(Trance(ASCII alpha,Create a new end tag token, set its tag name to the empty string. Reconsume in the tag name state. ), Trance(U+003E GREATER-THAN SIGN (&gt;),This is a missing-end-tag-name parse error. Switch to the data state.), Trance(EOF,This is an eof-before-tag-name parse error. Emit a U+003C LESS-THAN SIGN character token, a U+002F SOLIDUS character token and an end-of-file token. ), Trance(Anything else,This is an invalid-first-character-of-tag-name parse error. Create a comment token whose data is the empty string. Reconsume in the bogus comment state.)))
State(Tag name state,Consume the next input character:,List(Trance(U+0009 CHARACTER TABULATION (tab),Switch to the before attribute name state.), Trance(U+000A LINE FEED (LF),Switch to the before attribute name state.), Trance(U+000C FORM FEED (FF),Switch to the before attribute name state.), Trance(U+0020 SPACE,Switch to the before attribute name state.), Trance(U+002F SOLIDUS (/),Switch to the self-closing start tag state.), Trance(U+003E GREATER-THAN SIGN (&gt;),Switch to the data state. Emit the current tag token.), Trance(ASCII upper alpha,Append the lowercase version of the current input character (add 0x0020 to the character's code point) to the current tag token's tag name.), Trance(U+0000 NULL,This is an unexpected-null-character parse error. Append a U+FFFD REPLACEMENT CHARACTER character to the current tag token's tag name.), Trance(EOF,This is an eof-in-tag parse error. Emit an end-of-file token.), Trance(Anything else,Append the current input character to the current tag token's tag name.)))
State(RCDATA less-than sign state,Consume the next input character:,List(Trance(U+002F SOLIDUS (/),Set the temporary buffer to the empty string. Switch to the RCDATA end tag open state.), Trance(Anything else,Emit a U+003C LESS-THAN SIGN character token. Reconsume in the RCDATA state.)))
State(RCDATA end tag open state,Consume the next input character:,List(Trance(ASCII alpha,Create a new end tag token, set its tag name to the empty string. Reconsume in the RCDATA end tag name state. ), Trance(Anything else,Emit a U+003C LESS-THAN SIGN character token and a U+002F SOLIDUS character token. Reconsume in the RCDATA state.)))
State(RCDATA end tag name state,Consume the next input character:,List(Trance(U+0009 CHARACTER TABULATION (tab),If the current end tag token is an appropriate end tag token, then switch to the before attribute name state. Otherwise, treat it as per the "anything else" entry below.), Trance(U+000A LINE FEED (LF),If the current end tag token is an appropriate end tag token, then switch to the before attribute name state. Otherwise, treat it as per the "anything else" entry below.), Trance(U+000C FORM FEED (FF),If the current end tag token is an appropriate end tag token, then switch to the before attribute name state. Otherwise, treat it as per the "anything else" entry below.), Trance(U+0020 SPACE,If the current end tag token is an appropriate end tag token, then switch to the before attribute name state. Otherwise, treat it as per the "anything else" entry below.), Trance(U+002F SOLIDUS (/),If the current end tag token is an appropriate end tag token, then switch to the self-closing start tag state. Otherwise, treat it as per the "anything else" entry below.), Trance(U+003E GREATER-THAN SIGN (&gt;),If the current end tag token is an appropriate end tag token, then switch to the data state and emit the current tag token. Otherwise, treat it as per the "anything else" entry below.), Trance(ASCII upper alpha,Append the lowercase version of the current input character (add 0x0020 to the character's code point) to the current tag token's tag name. Append the current input character to the temporary buffer.), Trance(ASCII lower alpha,Append the current input character to the current tag token's tag name. Append the current input character to the temporary buffer.), Trance(Anything else,Emit a U+003C LESS-THAN SIGN character token, a U+002F SOLIDUS character token, and a character token for each of the characters in the temporary buffer (in the order they were added to the buffer). Reconsume in the RCDATA state.)))
State(RAWTEXT less-than sign state,Consume the next input character:,List(Trance(U+002F SOLIDUS (/),Set the temporary buffer to the empty string. Switch to the RAWTEXT end tag open state.), Trance(Anything else,Emit a U+003C LESS-THAN SIGN character token. Reconsume in the RAWTEXT state.)))
State(RAWTEXT end tag open state,Consume the next input character:,List(Trance(ASCII alpha,Create a new end tag token, set its tag name to the empty string. Reconsume in the RAWTEXT end tag name state. ), Trance(Anything else,Emit a U+003C LESS-THAN SIGN character token and a U+002F SOLIDUS character token. Reconsume in the RAWTEXT state.)))
State(RAWTEXT end tag name state,Consume the next input character:,List(Trance(U+0009 CHARACTER TABULATION (tab),If the current end tag token is an appropriate end tag token, then switch to the before attribute name state. Otherwise, treat it as per the "anything else" entry below.), Trance(U+000A LINE FEED (LF),If the current end tag token is an appropriate end tag token, then switch to the before attribute name state. Otherwise, treat it as per the "anything else" entry below.), Trance(U+000C FORM FEED (FF),If the current end tag token is an appropriate end tag token, then switch to the before attribute name state. Otherwise, treat it as per the "anything else" entry below.), Trance(U+0020 SPACE,If the current end tag token is an appropriate end tag token, then switch to the before attribute name state. Otherwise, treat it as per the "anything else" entry below.), Trance(U+002F SOLIDUS (/),If the current end tag token is an appropriate end tag token, then switch to the self-closing start tag state. Otherwise, treat it as per the "anything else" entry below.), Trance(U+003E GREATER-THAN SIGN (&gt;),If the current end tag token is an appropriate end tag token, then switch to the data state and emit the current tag token. Otherwise, treat it as per the "anything else" entry below.), Trance(ASCII upper alpha,Append the lowercase version of the current input character (add 0x0020 to the character's code point) to the current tag token's tag name. Append the current input character to the temporary buffer.), Trance(ASCII lower alpha,Append the current input character to the current tag token's tag name. Append the current input character to the temporary buffer.), Trance(Anything else,Emit a U+003C LESS-THAN SIGN character token, a U+002F SOLIDUS character token, and a character token for each of the characters in the temporary buffer (in the order they were added to the buffer). Reconsume in the RAWTEXT state.)))
State(Script data less-than sign state,Consume the next input character:,List(Trance(U+002F SOLIDUS (/),Set the temporary buffer to the empty string. Switch to the script data end tag open state.), Trance(U+0021 EXCLAMATION MARK (!),Switch to the script data escape start state. Emit a U+003C LESS-THAN SIGN character token and a U+0021 EXCLAMATION MARK character token.), Trance(Anything else,Emit a U+003C LESS-THAN SIGN character token. Reconsume in the script data state.)))
State(Script data end tag open state,Consume the next input character:,List(Trance(ASCII alpha,Create a new end tag token, set its tag name to the empty string. Reconsume in the script data end tag name state. ), Trance(Anything else,Emit a U+003C LESS-THAN SIGN character token and a U+002F SOLIDUS character token. Reconsume in the script data state.)))
State(Script data end tag name state,Consume the next input character:,List(Trance(U+0009 CHARACTER TABULATION (tab),If the current end tag token is an appropriate end tag token, then switch to the before attribute name state. Otherwise, treat it as per the "anything else" entry below.), Trance(U+000A LINE FEED (LF),If the current end tag token is an appropriate end tag token, then switch to the before attribute name state. Otherwise, treat it as per the "anything else" entry below.), Trance(U+000C FORM FEED (FF),If the current end tag token is an appropriate end tag token, then switch to the before attribute name state. Otherwise, treat it as per the "anything else" entry below.), Trance(U+0020 SPACE,If the current end tag token is an appropriate end tag token, then switch to the before attribute name state. Otherwise, treat it as per the "anything else" entry below.), Trance(U+002F SOLIDUS (/),If the current end tag token is an appropriate end tag token, then switch to the self-closing start tag state. Otherwise, treat it as per the "anything else" entry below.), Trance(U+003E GREATER-THAN SIGN (&gt;),If the current end tag token is an appropriate end tag token, then switch to the data state and emit the current tag token. Otherwise, treat it as per the "anything else" entry below.), Trance(ASCII upper alpha,Append the lowercase version of the current input character (add 0x0020 to the character's code point) to the current tag token's tag name. Append the current input character to the temporary buffer.), Trance(ASCII lower alpha,Append the current input character to the current tag token's tag name. Append the current input character to the temporary buffer.), Trance(Anything else,Emit a U+003C LESS-THAN SIGN character token, a U+002F SOLIDUS character token, and a character token for each of the characters in the temporary buffer (in the order they were added to the buffer). Reconsume in the script data state.)))
State(Script data escape start state,Consume the next input character:,List(Trance(U+002D HYPHEN-MINUS (-),Switch to the script data escape start dash state. Emit a U+002D HYPHEN-MINUS character token.), Trance(Anything else,Reconsume in the script data state.)))
State(Script data escape start dash state,Consume the next input character:,List(Trance(U+002D HYPHEN-MINUS (-),Switch to the script data escaped dash dash state. Emit a U+002D HYPHEN-MINUS character token.), Trance(Anything else,Reconsume in the script data state.)))
State(Script data escaped state,Consume the next input character:,List(Trance(U+002D HYPHEN-MINUS (-),Switch to the script data escaped dash state. Emit a U+002D HYPHEN-MINUS character token.), Trance(U+003C LESS-THAN SIGN (&lt;),Switch to the script data escaped less-than sign state.), Trance(U+0000 NULL,This is an unexpected-null-character parse error. Emit a U+FFFD REPLACEMENT CHARACTER character token.), Trance(EOF,This is an eof-in-script-html-comment-like-text parse error. Emit an end-of-file token.), Trance(Anything else,Emit the current input character as a character token.)))
